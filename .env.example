# Copy to .env.local and fill in real values. Never commit .env* files.

# Resend — Private Inquiry form delivery
RESEND_API_KEY=your_resend_api_key_here
INQUIRY_TO_EMAIL=inquiries@example.com
INQUIRY_FROM_EMAIL=onboarding@resend.dev

# Set to "1" ONLY by the test runner — stubs out Resend delivery.
# INQUIRY_TEST_MODE=1
